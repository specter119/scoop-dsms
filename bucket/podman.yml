version: 5.6.2
description: A tool for managing OCI containers and pods.
homepage: https://podman.io/
license: Apache-2.0
architecture:
  64bit:
    url: https://github.com/containers/podman/releases/download/v5.6.2/podman-remote-release-windows_amd64.zip
    hash: 9c86b2686dabc287af1de1b4fadc506310718bf84fccbd12f0831fc7726a7cd5
extract_dir: podman-5.6.2
pre_install: |
  if (Test-Path "$dir\usr\bin") {
    Move-Item "$dir\usr\bin\*" "$dir" -Force
    Remove-Item "$dir\usr" -Recurse -Force
  }
  if (Test-Path "$dir\docs") {
    Move-Item "$dir\docs\*" "$dir" -Force
    Remove-Item "$dir\docs" -Recurse -Force
  }
bin: podman.exe
checkver:
  github: https://github.com/containers/podman
autoupdate:
  architecture:
    64bit:
      url: https://github.com/containers/podman/releases/download/v$version/podman-remote-release-windows_amd64.zip
      hash:
        url: $baseurl/shasums
  extract_dir: podman-$version
